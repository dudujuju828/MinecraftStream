
#pragma once

// REVIEW: World is an empty class — the world is currently managed as a
// std::vector<Chunk> in main(). If you plan to use this class, it should own the
// chunk vector and provide methods like getChunkAt(x, z) for raycasting.
class World {
public:
    




private:
};